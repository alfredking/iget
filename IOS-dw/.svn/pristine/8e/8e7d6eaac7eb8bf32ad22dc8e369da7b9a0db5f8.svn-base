#ifndef HMAC_SHA256_H
#define HMAC_SHA256_H
#ifdef  __cplusplus
extern "C" {
#endif

void hmac_sha256
(
		const void *key, size_t key_len, const void *data, size_t data_len, void *ret_buf
//	char* k, /* secret key */
//	int lk, /* length of the key in bytes */
//	char* d, /* data */
//	int ld, /* length of data in bytes */
//	unsigned char* out /* output buffer, at least 32 bytes */
);

#ifdef  __cplusplus
}
#endif

#endif
